name: Release

on:
  push:
    branches:
      - master
  # Lets a maintainer re-fire the release from the Actions tab when a push
  # to master never produced a run (e.g. a GitHub Actions outage swallowed
  # the event) - there is otherwise no run to re-run in that situation.
  workflow_dispatch: {}

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  release:
    name: Semantic Release
    runs-on: ubuntu-latest
    # This prevents concurrent releases from clashing
    concurrency:
      group: release-${{ github.ref }}
      cancel-in-progress: false

    steps:
      - name: Generate Token
        id: generate_token
        uses: actions/create-github-app-token@v3
        with:
          app-id: ${{ secrets.RELEASE_APP_ID }}
          private-key: ${{ secrets.RELEASE_APP_KEY }}

      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          # We use the App token here so 'git push' works with the App's identity
          token: ${{ steps.generate_token.outputs.token }}
          persist-credentials: true

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build TypeScript
        run: npm run build

      - name: Run tests
        run: npm test

      - name: Create release tag and changelog
        uses: cycjimmy/semantic-release-action@v6
        with:
          extra_plugins: |
            @semantic-release/changelog@6
            @semantic-release/git@10
        env:
          # Crucial: Use the App token here as well
          GITHUB_TOKEN: ${{ steps.generate_token.outputs.token }}
