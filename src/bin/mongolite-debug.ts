#!/usr/bin/env node
/**
 * `mongolite-debug` is kept as an alias of `mongolite` so existing scripts and
 * muscle memory keep working. It runs the same guided explorer; pass `--repl`
 * for the original command-driven debugger (`.use`, `.find`, `.sql`, …).
 */
import './mongolite.js';
